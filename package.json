{
  "name": "nodara",
  "version": "0.1.0",
  "private": true,
  "description": "Nodara — Programmable On-Chain Automation. Reliable workflow execution through KeeperHub.",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev:backend": "npm run dev -w apps/backend",
    "dev:frontend": "npm run dev -w apps/frontend",
    "build:packages": "npm run build -w packages/shared -w packages/types -w packages/utils -w packages/blockchain -w packages/keeperhub -w packages/monitor",
    "build": "npm run build:packages && npm run build -w apps/backend -w apps/frontend",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "prisma:generate": "npm run prisma:generate -w apps/backend",
    "prisma:migrate": "npm run prisma:migrate -w apps/backend",
    "prisma:studio": "npm run prisma:studio -w apps/backend"
  },
  "engines": { "node": ">=20.0.0" },
  "devDependencies": { "typescript": "^5.6.3" }
}
